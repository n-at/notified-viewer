<ul class="pagination">
    {% for p in range(1, pages) %}
        <li class="{% if p==page %} active {% endif %}">
            <a href="?page={{ p-1 }}">{{ p }}</a>
        </li>
    {% endfor %}
</ul>